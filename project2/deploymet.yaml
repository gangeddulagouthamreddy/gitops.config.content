apiVersion: apps/v1
kind: Deployment
metadata:
  name: canvas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: canvas
  template:
    metadata:
      labels:
        app: canvas
    spec:
      containers:
        - name: web
          image: nginx:alpine
          ports:
            - containerPort: 80
